package akka.spring.config

/**
 *
 * The BFPP sifts through the beans that are defined and checks for the presence of annotations
 * and, if so present, replaces the classes with proxies delegating proxies.
 *
 * @author Josh Long
 */
class AkkaBeanFactoryPostProcessor {

}